import { QrCode } from "lucide-react";
import { createFileRoute, Link } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { activeOrder } from "@/data/qbite";
import { PageIntro, PickupDetails, ProgressSteps, QueueCard, StatusBadge } from "@/components/qbite/shared";
export const Route=createFileRoute("/tracking")({head:()=>({meta:[{title:"Track Order QB104 — QBite"},{name:"description",content:"Follow your campus food order and see the live pickup queue."},{property:"og:title",content:"Track Order QB104 — QBite"},{property:"og:description",content:"Live campus order progress and pickup details."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Tracking});
function Tracking(){return <div className="space-y-7"><PageIntro eyebrow="Live order" title={`Order ${activeOrder.id}`} description="Your food is being prepared. We’ll let you know when it’s ready." action={<StatusBadge status={activeOrder.status}/>}/><PickupDetails/><Card className="p-5 sm:p-7"><h2 className="text-lg font-bold">Order Progress</h2><div className="mt-6"><ProgressSteps status={activeOrder.status}/></div><div className="mt-7 rounded-lg bg-secondary p-4"><div className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-4"><div><p className="font-bold">Your food is being prepared.</p><p className="mt-1 text-sm text-muted-foreground">Estimated ready: <strong className="text-foreground">{activeOrder.readyAt}</strong></p></div><span className="text-2xl">🍜</span></div></div></Card><div className="grid gap-5 lg:grid-cols-2"><QueueCard compact/><Card className="flex flex-col justify-between border-primary/20 bg-primary/5 p-5"><div><p className="text-xs font-bold uppercase text-primary">Pickup pass</p><h2 className="mt-2 text-xl font-bold">Your QR will be ready soon</h2><p className="mt-2 text-sm text-muted-foreground">Open your digital ticket when the order status changes to Ready.</p></div><Button asChild variant="outline" className="mt-5"><Link to="/pickup"><QrCode/>Preview Pickup QR</Link></Button></Card></div></div>}
