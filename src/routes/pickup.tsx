import { CheckCircle2 } from "lucide-react";
import { createFileRoute } from "@tanstack/react-router";
import { Card } from "@/components/ui/card";
import { activeOrder } from "@/data/qbite";
export const Route=createFileRoute("/pickup")({head:()=>({meta:[{title:"Pickup Ticket QB104 — QBite"},{name:"description",content:"Digital campus pickup pass for QBite order QB104."},{property:"og:title",content:"Pickup Ticket QB104 — QBite"},{property:"og:description",content:"Show this campus pickup pass at the canteen counter."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Pickup});
function Pickup(){return <div className="mx-auto max-w-lg py-4"><Card className="overflow-hidden border-primary/30 shadow-lg"><div className="bg-foreground px-6 py-5 text-background"><div className="flex items-center justify-between"><span className="text-2xl font-black">QBite</span><span className="rounded-md bg-primary px-3 py-1 text-xs font-black text-primary-foreground">READY</span></div><p className="mt-7 text-xs font-bold uppercase text-background/60">Campus pickup pass</p><h1 className="mt-1 text-3xl font-black">ORDER {activeOrder.id}</h1></div><div className="p-6"><div className="grid grid-cols-2 gap-4"><div><p className="text-xs font-bold uppercase text-muted-foreground">Pickup</p><p className="mt-1 font-bold">{activeOrder.pickup}</p></div><div><p className="text-xs font-bold uppercase text-muted-foreground">Counter</p><p className="mt-1 font-bold">Main Canteen · 2</p></div></div><div className="my-6 border-t border-dashed"/><div className="mx-auto grid aspect-square w-full max-w-[280px] grid-cols-11 gap-0.5 bg-foreground p-3" aria-label="Demo pickup QR code">{Array.from({length:121}).map((_,i)=><span key={i} className={(i*7+i%5)%4===0||i<12||i>109||i%11===0?"bg-background":"bg-foreground"}/>)}</div><p className="mt-5 text-center text-lg font-bold">Show this QR at the counter</p><p className="mt-1 text-center text-sm text-muted-foreground">Keep the code visible for a fast handoff.</p><div className="mt-6 flex items-center justify-center gap-2 rounded-lg bg-status-ready p-3 font-semibold text-status-ready-foreground"><CheckCircle2 className="h-5 w-5"/>Ready for pickup</div></div></Card></div>}
